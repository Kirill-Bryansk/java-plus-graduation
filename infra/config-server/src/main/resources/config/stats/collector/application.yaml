# Kafka: адрес брокера и имя топика
kafka:
  bootstrap:
    servers: localhost:9092
  topics:
    stats-user-action-v1: stats.user-actions.v1

# gRPC-сервер: случайный порт (несколько реплик)
grpc:
  server:
    port: 0
